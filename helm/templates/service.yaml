apiVersion: v1
kind: Service
metadata:
  name: {{ include "arc-insights.fullname" . }}
  labels:
    {{- include "arc-insights.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arc-insights.selectorLabels" . | nindent 4 }}
